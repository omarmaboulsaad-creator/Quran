 "use client";
import {useState} from "react";
export default function Recitation(){const [file,setFile]=useState<File|null>(null);const [result,setResult]=useState<any>(null);const [busy,setBusy]=useState(false);
async function analyze(){if(!file)return;setBusy(true);const f=new FormData();f.append("audio",file);f.append("surah","1");f.append("startAyah","1");f.append("endAyah","7");const r=await fetch("/api/recitation/analyze",{method:"POST",body:f});const j=await r.json();setResult(j);setBusy(false)}
return <><div className="top"><h1>التلاوة والتحليل</h1></div><div className="card"><h2>تحليل تلاوتك</h2><p className="muted">ارفع تسجيلًا بصيغة صوتية. محرك التحليل يحدد النص المسموع ويقارن الآيات ويعيد الأخطاء.</p><input type="file" accept="audio/*" onChange={e=>setFile(e.target.files?.[0]||null)}/><br/><br/><button className="btn" disabled={!file||busy} onClick={analyze}>{busy?"جاري التحليل...":"🤖 تحليل التلاوة"}</button></div>{result&&<><br/><div className="card"><h2>النتيجة</h2><div className="grid"><div><b>الدقة</b><h2>{result.accuracy??"-"}%</h2></div><div><b>الأخطاء</b><h2>{result.issues?.length??0}</h2></div><div><b>النص المسموع</b><p>{result.transcript||"—"}</p></div></div></div></>}</>}
